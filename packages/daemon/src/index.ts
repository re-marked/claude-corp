// @agentcorp/daemon — Layer 2+ will implement:
// - Router (fs.watch + @mention dispatch)
// - Process Manager (execa + OpenClaw spawning)
// - Git Manager (periodic commits)

export const VERSION = '0.0.1';
