node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
*.js.map
*.d.ts.map

# Docs (private design spec — local only)
docs/

# Claude
.claude/
# Exception: the audit-gate probe's settings.json is its load-bearing
# piece (wires the Stop hook to the probe script). Track it explicitly.
!scripts/audit-gate-probe/.claude/
!scripts/audit-gate-probe/.claude/settings.json
